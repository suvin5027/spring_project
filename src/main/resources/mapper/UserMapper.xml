<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
		"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.colar.spring_project.mapper.UserMapper">

	<!-- 로그인 — 아이디로 조회 후 Java에서 비밀번호 비교 -->
	<select id="login" parameterType="com.colar.spring_project.dto.UserDTO" resultType="com.colar.spring_project.dto.UserDTO">
		SELECT USER_SEQ, USER_ID, USER_NAME, NICKNAME, USER_ROLE, PASSWORD
		FROM TB_USER
		WHERE USER_ID = #{userId}
	</select>

	<!-- 회원가입 -->
	<insert id="register" parameterType="com.colar.spring_project.dto.UserDTO">
		INSERT INTO TB_USER (USER_ID, USER_NAME, NICKNAME, EMAIL, PASSWORD, USER_ROLE)
		VALUES (#{userId}, #{userName}, #{nickname}, #{email}, #{password}, 'USER')
	</insert>

	<!-- 아이디로 회원 조회 -->
	<select id="findByUserId" parameterType="String" resultType="com.colar.spring_project.dto.UserDTO">
		SELECT USER_SEQ, USER_ID, USER_NAME, NICKNAME, EMAIL, USER_ROLE
		FROM TB_USER
		WHERE USER_ID = #{userId}
	</select>

	<!-- 아이디 찾기 — 이름 + 이메일 일치 시 아이디 반환 -->
	<select id="findIdByNameAndEmail" parameterType="com.colar.spring_project.dto.UserDTO" resultType="String">
		SELECT USER_ID
		FROM TB_USER
		WHERE USER_NAME = #{userName}
		AND EMAIL = #{email}
	</select>

	<!-- 비밀번호 재설정 — 아이디 + 이메일 일치 시 비밀번호 변경 -->
	<update id="updatePasswordByIdAndEmail" parameterType="com.colar.spring_project.dto.UserDTO">
		UPDATE TB_USER
		SET PASSWORD = #{password}
		WHERE USER_ID = #{userId}
		AND EMAIL = #{email}
	</update>

</mapper>
