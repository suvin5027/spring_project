# Spring Boot 설정 — spring_project
# IDE: VSCode / Spring Boot 3.5.14 / Java 21 / Gradle

## application.properties

###properties
spring.application.name=spring_project

# Server
server.port=8080

# MariaDB
spring.datasource.driver-class-name=org.mariadb.jdbc.Driver
spring.datasource.url=jdbc:mariadb://localhost:3306/react_project
spring.datasource.username=root
spring.datasource.password=root

# MyBatis
mybatis.mapper-locations=classpath:mapper/**/*.xml
mybatis.configuration.map-underscore-to-camel-case=true

## 프로젝트 정보

#| 항목 | 값 |
#|------|-----|
#| 프로젝트 위치 | `C:\dev\workspace\spring_project` |
#| Group Id | `com.suvin` |
#| Artifact Id | `spring_project` |
#| Packaging | Jar |
#| Java | 21 |
#| Dependencies | Spring Web, MyBatis Framework, MariaDB Driver, Lombok |
#| React 연동 포트 | React 3000 ↔ Spring 8080 |
